<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.loginandsignup">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginAndSignUp"
        tools:targetApi="31">

        <!-- FileProvider for camera image saving -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Activities -->
        <activity android:name=".MemberDetailActivity" android:windowSoftInputMode="adjustPan" android:requestLegacyExternalStorage="true" />
        <activity android:name=".EditFamilyMemberActivity" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".SplashActivity" android:exported="true" tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeScreen" />
        <activity android:name=".MainScreenActivity" />
        <activity android:name=".SignupActivity" android:windowSoftInputMode="adjustPan" android:parentActivityName=".MainScreenActivity" />
        <activity android:name=".LoginActivity" android:windowSoftInputMode="adjustPan" android:parentActivityName=".MainScreenActivity" />
    </application>
</manifest>
